{% capture redirectScript %}{% render 'bsscommerce-redirect-product-page-logic', product: product %}{% endcapture %}{% assign redirectScript = redirectScript | strip %}{% unless redirectScript contains "Liquid error" %}{% if redirectScript != "" %}<script id="bss-redirect" type="text/javascript">window.location.replace("{{ redirectScript }}")</script>{% endif %}{% endunless %}{%- assign meta_block = section.blocks | where: 'type', 'product_meta' | first -%}
{%- assign share_buttons_block = section.blocks | where: 'type', 'share_buttons' | first -%}
{%- assign inventory_block = section.blocks | where: 'type', 'inventory' | first -%}
{%- assign buy_buttons_block = section.blocks | where: 'type', 'buy_buttons' | first -%}

{%- capture section_settings -%}
{
  "enableHistoryState": true,
  "templateSuffix": {{ product.template_suffix | json }},
  "showInventoryQuantity": {% if inventory_block != blank %}true{% else %}false{% endif %},
  "showSku": {{ meta_block.settings.show_sku | json }},
  "stackProductImages": {{ section.settings.stack_images | json }},
  "showThumbnails": {{ section.settings.show_thumbnails | json }},
  "enableVideoLooping": {{ section.settings.enable_video_looping | json }},
  "inventoryQuantityThreshold": {{ inventory_block.settings.inventory_quantity_threshold | default: 0 }},
  "showPriceInButton": {{ buy_buttons_block.settings.show_price_in_button | json }},
  "enableImageZoom": {{ section.settings.enable_image_zoom | json }},
  "showPaymentButton": {{ buy_buttons_block.settings.show_payment_button | json }},
  "useAjaxCart": {% if settings.cart_type == 'drawer' %}true{% else %}false{% endif %}
}
{%- endcapture -%}

<script>
  (() => {
    let items = JSON.parse(localStorage.getItem('recentlyViewedProducts') || '[]');
    if (!items.includes({{ product.id }})) {
      items.unshift({{ product.id }});
    }
    try {
      localStorage.setItem('recentlyViewedProducts', JSON.stringify(items.slice(0, 8)));
    } catch (error) {}
  })();
</script>

<section class="Product product-page-v1" data-section-id="{{ section.id }}" data-section-type="product" data-section-settings='{{ section_settings }}'>

  {%- comment -%} Hidden product data for theme JS (variant switching, ajax cart) {%- endcomment -%}
  {%- form 'product', product, class: 'ProductForm', style: 'display:none' -%}
    {%- render 'product-data', product: product -%}
    <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}" data-product-select>
  {%- endform -%}

  {%- comment -%}----------------------------------------------------
  1. HERO SECTION
  ----------------------------------------------------{%- endcomment -%}

  <div class="ppv1-hero">
    <div class="ppv1-hero__gallery">
      {%- if product.featured_media -%}
        {%- assign hero_url = product.featured_media | img_url: '1x1' | replace: '_1x1.', '_{width}x.' -%}
        <img class="ppv1-hero__main-img Image--lazyLoad Image--fadeIn"
             data-src="{{ hero_url }}"
             data-widths="[600, 800, 1000, 1200]"
             data-sizes="auto"
             alt="{{ product.featured_media.alt | default: product.title | escape }}">
        <span class="Image__Loader"></span>
      {%- endif -%}
    </div>

    <div class="ppv1-hero__info">
      {%- if product.vendor != blank -%}
        <span class="ppv1-brand">{{ product.vendor | upcase }}</span>
      {%- endif -%}

      <h1 class="ppv1-title">{{ product.title }}</h1>

      {%- comment -%} Metadata from product tags in "Key: Value" format {%- endcomment -%}
      {%- assign has_meta_tags = false -%}
      {%- for tag in product.tags -%}
        {%- if tag contains ': ' -%}
          {%- assign has_meta_tags = true -%}
        {%- endif -%}
      {%- endfor -%}

      {%- if has_meta_tags -%}
        <dl class="ppv1-meta">
          {%- for tag in product.tags -%}
            {%- if tag contains ': ' -%}
              {%- assign tag_parts = tag | split: ': ' -%}
              <dt class="ppv1-meta__key">{{ tag_parts[0] }}</dt>
              <dd class="ppv1-meta__val">{{ tag_parts[1] }}</dd>
            {%- endif -%}
          {%- endfor -%}
        </dl>
      {%- endif -%}

      <hr class="ppv1-divider">

      <div class="ppv1-price">{{ product.selected_or_first_available_variant.price | money }}</div>

      {%- form 'product', product, class: 'ppv1-form' -%}
        <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}" data-product-select>
        <div class="ppv1-cta-row">
          {%- if product.selected_or_first_available_variant.available -%}
            <button type="submit" class="ppv1-btn ppv1-btn--primary" data-action="add-to-cart">
              ADD TO CART &mdash; {{ product.selected_or_first_available_variant.price | money }}
            </button>
          {%- else -%}
            <button type="button" class="ppv1-btn ppv1-btn--primary" disabled>SOLD OUT</button>
          {%- endif -%}
          <a href="https://wa.me/85293476168?text={{ 'Hello, I would like to make an offer on the ' | append: product.title | append: '.' | url_encode }}"
             target="_blank"
             rel="noopener"
             class="ppv1-btn ppv1-btn--secondary">MAKE AN OFFER</a>
        </div>
      {%- endform -%}
    </div>
  </div>

  {%- comment -%}----------------------------------------------------
  2. IMAGE CAROUSEL STRIP
  ----------------------------------------------------{%- endcomment -%}

  {%- if product.media.size > 1 -%}
    <div class="ppv1-strip">
      <button class="ppv1-strip__arrow ppv1-strip__arrow--prev" id="ppv1-prev" type="button" aria-label="Previous images">&#8592;</button>
      <div class="ppv1-strip__track" id="ppv1-track">
        {%- for media in product.media -%}
          {%- if media.media_type == 'image' -%}
            {%- assign strip_url = media | img_url: '1x1' | replace: '_1x1.', '_{width}x.' -%}
            <div class="ppv1-strip__slide">
              <img class="ppv1-strip__img Image--lazyLoad Image--fadeIn"
                   data-src="{{ strip_url }}"
                   data-widths="[400, 600, 800]"
                   data-sizes="auto"
                   alt="{{ media.alt | default: product.title | escape }}">
              <span class="Image__Loader"></span>
            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>
      <button class="ppv1-strip__arrow ppv1-strip__arrow--next" id="ppv1-next" type="button" aria-label="Next images">&#8594;</button>
    </div>
  {%- endif -%}

  {%- comment -%}----------------------------------------------------
  3. ALL DETAILS SECTION
  ----------------------------------------------------{%- endcomment -%}

  <div class="ppv1-details">
    <div class="ppv1-details__left">
      <h2 class="ppv1-section-heading">ALL DETAILS</h2>
      {%- if product.description != blank -%}
        <div class="ppv1-description Rte">{{ product.description }}</div>
      {%- endif -%}
      <a href="https://wa.me/85293476168?text={{ 'Hello, I am interested in the ' | append: product.title | append: '!' | url_encode }}"
         target="_blank"
         rel="noopener"
         class="ppv1-btn ppv1-btn--tertiary">CHAT TO OUR TEAM</a>
    </div>

    <div class="ppv1-details__right">
      <div class="ppv1-accordion">

        {%- comment -%} Accordion: Product Details {%- endcomment -%}
        <div class="ppv1-accordion__item">
          <button class="ppv1-accordion__trigger" type="button" aria-expanded="false">
            <span>Product Details</span>
            <span class="ppv1-accordion__icon" aria-hidden="true">+</span>
          </button>
          <div class="ppv1-accordion__body">
            <div class="ppv1-accordion__content">
              <dl class="ppv1-detail-list">
                {%- if product.vendor != blank -%}
                  <dt>Brand</dt><dd>{{ product.vendor }}</dd>
                {%- endif -%}
                {%- for tag in product.tags -%}
                  {%- if tag contains ': ' -%}
                    {%- assign tag_parts = tag | split: ': ' -%}
                    <dt>{{ tag_parts[0] }}</dt><dd>{{ tag_parts[1] }}</dd>
                  {%- endif -%}
                {%- endfor -%}
              </dl>
            </div>
          </div>
        </div>

        {%- comment -%} Accordion: Delivery {%- endcomment -%}
        {%- assign delivery_block = blank -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'content' and block.settings.title contains 'Delivery' -%}
            {%- assign delivery_block = block -%}
          {%- endif -%}
        {%- endfor -%}
        <div class="ppv1-accordion__item">
          <button class="ppv1-accordion__trigger" type="button" aria-expanded="false">
            <span>Delivery</span>
            <span class="ppv1-accordion__icon" aria-hidden="true">+</span>
          </button>
          <div class="ppv1-accordion__body">
            <div class="ppv1-accordion__content">
              {%- if delivery_block != blank -%}
                <div class="Rte">{{ delivery_block.settings.content }}</div>
              {%- else -%}
                <p>DHL delivery within 1–2 working days. For local pick up in London please contact us directly. Free shipping. You will receive your receipt via email.</p>
              {%- endif -%}
            </div>
          </div>
        </div>

        {%- comment -%} Accordion: Payment Methods {%- endcomment -%}
        {%- assign payment_block = blank -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'content' and block.settings.title contains 'Payment' -%}
            {%- assign payment_block = block -%}
          {%- endif -%}
        {%- endfor -%}
        <div class="ppv1-accordion__item">
          <button class="ppv1-accordion__trigger" type="button" aria-expanded="false">
            <span>Payment Methods</span>
            <span class="ppv1-accordion__icon" aria-hidden="true">+</span>
          </button>
          <div class="ppv1-accordion__body">
            <div class="ppv1-accordion__content">
              {%- if payment_block != blank -%}
                <div class="Rte">{{ payment_block.settings.content }}</div>
              {%- else -%}
                <p>We accept Visa, Mastercard, Maestro, American Express, Apple Pay &amp; PayPal. For WeChat Pay or Alipay, please contact us directly.</p>
              {%- endif -%}
            </div>
          </div>
        </div>

      </div>
    </div>
  </div>

  {%- comment -%}----------------------------------------------------
  4. DISCOVER THE STORY SECTION
  ----------------------------------------------------{%- endcomment -%}

  {%- assign story_media = product.media[1] -%}
  {%- if story_media == blank or story_media.media_type != 'image' -%}
    {%- assign story_media = product.media[0] -%}
  {%- endif -%}

  <div class="ppv1-story">
    <div class="ppv1-story__image">
      {%- if story_media and story_media.media_type == 'image' -%}
        {%- assign story_url = story_media | img_url: '1x1' | replace: '_1x1.', '_{width}x.' -%}
        <img class="ppv1-story__img Image--lazyLoad Image--fadeIn"
             data-src="{{ story_url }}"
             data-widths="[600, 800, 1000]"
             data-sizes="auto"
             alt="{{ story_media.alt | default: product.title | escape }}">
        <span class="Image__Loader"></span>
      {%- endif -%}
    </div>
    <div class="ppv1-story__content">
      <h2 class="ppv1-section-heading">DISCOVER THE STORY</h2>
      {%- if product.description != blank -%}
        <div class="ppv1-story__text">{{ product.description | strip_html | truncatewords: 70 }}</div>
      {%- endif -%}
      <a href="{{ product.url }}" class="ppv1-btn ppv1-btn--tertiary">READ MORE</a>
    </div>
  </div>

</section>

{%- comment -%}
--------------------------------------------------------------------------------------------------------------------
PHOTO SWIPE
--------------------------------------------------------------------------------------------------------------------
{%- endcomment -%}

{%- if section.settings.enable_image_zoom -%}
  <div class="pswp" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="pswp__bg"></div>
    <div class="pswp__scroll-wrap">
      <div class="pswp__container">
        <div class="pswp__item"></div>
        <div class="pswp__item"></div>
        <div class="pswp__item"></div>
      </div>
      <div class="pswp__ui pswp__ui--hidden">
        <button class="pswp__button pswp__button--prev RoundButton" data-animate-left title="{{ 'product.slideshow.previous' | t }}">{% render 'icon' with 'arrow-left' %}</button>
        <button class="pswp__button pswp__button--close RoundButton RoundButton--large" data-animate-bottom title="{{ 'product.slideshow.close' | t }}">{% render 'icon' with 'close' %}</button>
        <button class="pswp__button pswp__button--next RoundButton" data-animate-right title="{{ 'product.slideshow.next' | t }}">{% render 'icon' with 'arrow-right' %}</button>
      </div>
    </div>
  </div>
{%- endif -%}

<script>
  window.ShopifyXR=window.ShopifyXR||function(){(ShopifyXR.q=ShopifyXR.q||[]).push(arguments)};
  ShopifyXR('addModels', {{ product.media | where: 'media_type', 'model' | json }});
</script>

<style>
/* =====================================================
   PRODUCT PAGE V1 — SCOPED STYLES
   ===================================================== */

.product-page-v1 {
  font-family: 'Jost', sans-serif;
  color: #1a1a1a;
  background: #ffffff;
}

/* ---- HERO ---- */
.ppv1-hero {
  display: flex;
  min-height: 640px;
}

.ppv1-hero__gallery {
  flex: 0 0 60%;
  position: relative;
  background: #f5f5f5;
  overflow: hidden;
}

.ppv1-hero__main-img {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.ppv1-hero__info {
  flex: 0 0 40%;
  padding: 64px 48px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  border-left: 1px solid #e5e5e5;
}

.ppv1-brand {
  font-family: 'Jost', sans-serif;
  font-size: 10px;
  font-weight: 400;
  letter-spacing: 0.22em;
  color: #9a9a9a;
  text-transform: uppercase;
  display: block;
  margin-bottom: 10px;
}

.ppv1-title {
  font-family: 'Jost', sans-serif;
  font-size: 22px;
  font-weight: 300;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: #1a1a1a;
  line-height: 1.3;
  margin: 0 0 24px;
}

/* Metadata key/value grid */
.ppv1-meta {
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 6px 20px;
  margin: 0 0 24px;
}

.ppv1-meta__key {
  font-family: 'Jost', sans-serif;
  font-size: 11px;
  font-weight: 400;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: #9a9a9a;
  white-space: nowrap;
}

.ppv1-meta__val {
  font-family: 'Jost', sans-serif;
  font-size: 11px;
  font-weight: 400;
  letter-spacing: 0.04em;
  color: #1a1a1a;
  margin: 0;
}

.ppv1-divider {
  border: none;
  border-top: 1px solid #e5e5e5;
  margin: 0 0 24px;
}

.ppv1-price {
  font-family: 'Jost', sans-serif;
  font-size: 20px;
  font-weight: 300;
  letter-spacing: 0.06em;
  color: #1a1a1a;
  margin-bottom: 28px;
}

/* Buttons */
.ppv1-cta-row {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.ppv1-btn {
  font-family: 'Jost', sans-serif;
  font-size: 11px;
  font-weight: 400;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 14px 24px;
  border-radius: 0;
  cursor: pointer;
  transition: background-color 0.2s ease, color 0.2s ease, border-color 0.2s ease;
  white-space: nowrap;
}

.ppv1-btn--primary {
  background: #8B7355;
  color: #ffffff;
  border: 1px solid #8B7355;
}

.ppv1-btn--primary:hover {
  background: #7a6245;
  border-color: #7a6245;
}

.ppv1-btn--primary:disabled {
  background: #cccccc;
  border-color: #cccccc;
  cursor: not-allowed;
}

.ppv1-btn--secondary {
  background: #ffffff;
  color: #1a1a1a;
  border: 1px solid #1a1a1a;
}

.ppv1-btn--secondary:hover {
  background: #f5f5f5;
}

.ppv1-btn--tertiary {
  background: #ffffff;
  color: #1a1a1a;
  border: 1px solid #e5e5e5;
  font-size: 10px;
  letter-spacing: 0.16em;
  padding: 12px 20px;
  margin-top: 24px;
  align-self: flex-start;
}

.ppv1-btn--tertiary:hover {
  border-color: #1a1a1a;
}

/* ---- CAROUSEL STRIP ---- */
.ppv1-strip {
  position: relative;
  background: #f5f5f5;
  border-top: 1px solid #e5e5e5;
}

.ppv1-strip__track {
  display: flex;
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  scrollbar-width: none;
  -ms-overflow-style: none;
}

.ppv1-strip__track::-webkit-scrollbar {
  display: none;
}

.ppv1-strip__slide {
  flex: 0 0 calc(100% / 3);
  scroll-snap-align: start;
  overflow: hidden;
  position: relative;
}

.ppv1-strip__img {
  width: 100%;
  height: 300px;
  object-fit: cover;
  display: block;
  transition: transform 0.4s ease;
}

.ppv1-strip__slide:hover .ppv1-strip__img {
  transform: scale(1.03);
}

.ppv1-strip__arrow {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  z-index: 2;
  background: rgba(255,255,255,0.9);
  border: 1px solid #e5e5e5;
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  font-size: 16px;
  color: #1a1a1a;
  transition: background 0.2s ease;
}

.ppv1-strip__arrow:hover {
  background: #ffffff;
}

.ppv1-strip__arrow--prev {
  left: 16px;
}

.ppv1-strip__arrow--next {
  right: 16px;
}

/* ---- ALL DETAILS ---- */
.ppv1-details {
  max-width: 1200px;
  margin: 0 auto;
  padding: 80px 40px;
  display: flex;
  gap: 80px;
  align-items: flex-start;
}

.ppv1-details__left {
  flex: 0 0 38%;
  display: flex;
  flex-direction: column;
}

.ppv1-details__right {
  flex: 1;
  border-left: 1px solid #e5e5e5;
  padding-left: 80px;
}

.ppv1-section-heading {
  font-family: 'Jost', sans-serif;
  font-size: 11px;
  font-weight: 400;
  letter-spacing: 0.22em;
  text-transform: uppercase;
  color: #1a1a1a;
  margin: 0 0 28px;
}

.ppv1-description {
  font-family: 'Jost', sans-serif;
  font-size: 13px;
  font-weight: 300;
  line-height: 1.8;
  color: #444444;
  margin-bottom: 0;
}

/* Accordion */
.ppv1-accordion__item {
  border-bottom: 1px solid #e5e5e5;
}

.ppv1-accordion__trigger {
  width: 100%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 18px 0;
  background: none;
  border: none;
  cursor: pointer;
  font-family: 'Jost', sans-serif;
  font-size: 11px;
  font-weight: 400;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: #1a1a1a;
  text-align: left;
}

.ppv1-accordion__trigger:hover {
  color: #8B7355;
}

.ppv1-accordion__icon {
  font-size: 18px;
  font-weight: 300;
  line-height: 1;
  transition: transform 0.25s ease;
  flex-shrink: 0;
  margin-left: 12px;
}

.ppv1-accordion__item.is-open .ppv1-accordion__icon {
  transform: rotate(45deg);
}

.ppv1-accordion__body {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease;
}

.ppv1-accordion__item.is-open .ppv1-accordion__body {
  max-height: 600px;
}

.ppv1-accordion__content {
  padding-bottom: 20px;
  font-family: 'Jost', sans-serif;
  font-size: 13px;
  font-weight: 300;
  line-height: 1.8;
  color: #444444;
}

.ppv1-accordion__content p {
  margin: 0 0 8px;
}

.ppv1-accordion__content p:last-child {
  margin-bottom: 0;
}

/* Product details key/value list in accordion */
.ppv1-detail-list {
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 6px 24px;
  margin: 0;
}

.ppv1-detail-list dt {
  font-weight: 400;
  letter-spacing: 0.08em;
  color: #9a9a9a;
  font-size: 11px;
  text-transform: uppercase;
  white-space: nowrap;
}

.ppv1-detail-list dd {
  font-size: 13px;
  color: #1a1a1a;
  margin: 0;
}

/* ---- DISCOVER THE STORY ---- */
.ppv1-story {
  display: flex;
  min-height: 540px;
  border-top: 1px solid #e5e5e5;
}

.ppv1-story__image {
  flex: 0 0 50%;
  position: relative;
  background: #f0ebe3;
  overflow: hidden;
}

.ppv1-story__img {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.ppv1-story__content {
  flex: 0 0 50%;
  background: #F5F0E8;
  padding: 80px 60px;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.ppv1-story__text {
  font-family: 'Jost', sans-serif;
  font-size: 14px;
  font-weight: 300;
  line-height: 1.9;
  color: #444444;
  margin-bottom: 0;
}

/* ---- MOBILE ---- */
@media screen and (max-width: 900px) {
  .ppv1-hero {
    flex-direction: column;
    min-height: auto;
  }

  .ppv1-hero__gallery {
    flex: none;
    height: 65vw;
    min-height: 320px;
  }

  .ppv1-hero__info {
    flex: none;
    padding: 36px 24px 48px;
    border-left: none;
    border-top: 1px solid #e5e5e5;
  }

  .ppv1-title {
    font-size: 18px;
  }

  .ppv1-cta-row {
    flex-direction: column;
  }

  .ppv1-btn {
    width: 100%;
  }

  .ppv1-strip__slide {
    flex: 0 0 82%;
  }

  .ppv1-strip__img {
    height: 240px;
  }

  .ppv1-details {
    flex-direction: column;
    padding: 48px 24px;
    gap: 48px;
  }

  .ppv1-details__left {
    flex: none;
  }

  .ppv1-details__right {
    border-left: none;
    padding-left: 0;
    border-top: 1px solid #e5e5e5;
    padding-top: 48px;
    width: 100%;
  }

  .ppv1-story {
    flex-direction: column;
    min-height: auto;
  }

  .ppv1-story__image {
    flex: none;
    height: 70vw;
    min-height: 300px;
  }

  .ppv1-story__content {
    flex: none;
    padding: 48px 24px;
  }
}
</style>

<script>
(function() {
  // Accordion
  document.querySelectorAll('.ppv1-accordion__trigger').forEach(function(btn) {
    btn.addEventListener('click', function() {
      var item = this.closest('.ppv1-accordion__item');
      var isOpen = item.classList.toggle('is-open');
      this.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
    });
  });

  // Carousel strip arrows
  var track = document.getElementById('ppv1-track');
  var prevBtn = document.getElementById('ppv1-prev');
  var nextBtn = document.getElementById('ppv1-next');

  if (track && prevBtn && nextBtn) {
    function getSlideWidth() {
      var slide = track.querySelector('.ppv1-strip__slide');
      return slide ? slide.offsetWidth : track.offsetWidth / 3;
    }
    prevBtn.addEventListener('click', function() {
      track.scrollBy({ left: -getSlideWidth(), behavior: 'smooth' });
    });
    nextBtn.addEventListener('click', function() {
      track.scrollBy({ left: getSlideWidth(), behavior: 'smooth' });
    });
  }
})();
</script>

{% schema %}
{
  "name": "Product page",
  "class": "shopify-section--bordered",
  "blocks": [
    {
      "type": "product_meta",
      "name": "Product meta",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_vendor",
          "label": "Show vendor",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "show_sku",
          "label": "Show SKU",
          "default": false
        },
        {
          "type": "checkbox",
          "id": "show_product_rating",
          "label": "Show product rating",
          "info": "To display a rating, add a product rating app. [Learn more](https://help.shopify.com/en/manual/products/product-reviews/installation)",
          "default": false
        },
        {
          "type": "checkbox",
          "id": "show_taxes_included",
          "label": "Show price taxes notice",
          "default": false
        }
      ]
    },
    {
      "type": "variant_selector",
      "name": "Variant selector",
      "limit": 1,
      "settings": [
        {
          "type": "select",
          "id": "selector_mode",
          "label": "Selector type",
          "options": [
            {
              "value": "block",
              "label": "Block"
            },
            {
              "value": "dropdown",
              "label": "Dropdown"
            }
          ],
          "default": "dropdown"
        },
        {
          "type": "checkbox",
          "id": "show_color_swatch",
          "label": "Show color swatch",
          "default": false,
          "info": "Some colors appear white? [Learn more](http://support.maestrooo.com/article/80-product-upgrading-custom-color-for-color-swatch)."
        },
        {
          "type": "checkbox",
          "id": "show_color_carousel",
          "label": "Show color carousel",
          "info": "A pop-up selector with variant images for choosing colors. Only enables when color swatch is disabled.",
          "default": false
        },
        {
          "type": "page",
          "id": "size_chart",
          "label": "Size chart",
          "info": "Show along option named Size."
        }
      ]
    },
    {
      "type": "share_buttons",
      "name": "Share buttons",
      "limit": 1
    },
    {
      "type": "inventory",
      "name": "Inventory",
      "limit": 1,
      "settings": [
        {
          "type": "range",
          "id": "inventory_quantity_threshold",
          "label": "Inventory quantity threshold",
          "info": "Only show inventory quantity if below threshold. Choose 0 to always show.",
          "min": 0,
          "max": 50,
          "step": 1,
          "default": 0
        }
      ]
    },
    {
      "type": "buy_buttons",
      "name": "Buy buttons",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_price_in_button",
          "label": "Show price in add to cart button",
          "default": false
        },
        {
          "type": "checkbox",
          "id": "show_payment_button",
          "label": "Show dynamic checkout button",
          "info": "Each customer will see their preferred payment method from those available on your store, such as PayPal or Apple Pay. [Learn more](https://help.shopify.com/manual/using-themes/change-the-layout/dynamic-checkout)",
          "default": true
        }
      ]
    },
    {
      "type": "description",
      "name": "Description",
      "limit": 1
    },
    {
      "type": "liquid",
      "name": "Liquid",
      "settings": [
        {
          "type": "paragraph",
          "content": "This Liquid code will show on the right part of the product page."
        },
        {
          "type": "liquid",
          "id": "liquid",
          "label": "Liquid"
        }
      ]
    },
    {
      "type": "quantity_selector",
      "name": "Quantity selector",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_label",
          "label": "Show label",
          "default": false
        }
      ]
    },
    {
      "type": "text",
      "name": "Text",
      "settings": [
        {
          "type": "paragraph",
          "content": "This text will show on the right part of the product page."
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text"
        }
      ]
    },
    {
      "type": "store_pickup",
      "name": "Local pickup availability",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "Show customers where they can pick up the product. [Learn more](https://help.shopify.com/en/manual/shipping/setting-up-and-managing-your-shipping/local-methods/local-pickup#show-pickup-availability-to-your-customers)"
        }
      ]
    },
    {
      "type": "@app"
    },
    {
      "name": "Content",
      "type": "content",
      "settings": [
        {
          "type": "paragraph",
          "content": "This content will show on the left part, below the product images."
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title"
        },
        {
          "type": "richtext",
          "id": "content",
          "label": "Content"
        },
        {
          "type": "page",
          "id": "page",
          "label": "Content from page",
          "info": "If specified, takes precedence over inline content."
        }
      ]
    },
    {
      "name": "Reviews",
      "type": "reviews",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "To show reviews, install [Shopify Product Reviews](https://apps.shopify.com/product-reviews). This content will show on the left part, below the product images. "
        }
      ]
    },
    {
      "name": "Buy it with",
      "type": "buy_it_with",
      "limit": 1,
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Product"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Heading",
          "default": "Buy it with"
        }
      ]
    }
  ],
  "settings": [
    {
      "type": "header",
      "content": "Media"
    },
    {
      "type": "paragraph",
      "content": "Learn more about [media types](https://help.shopify.com/en/manual/products/product-media)"
    },
    {
      "type": "select",
      "id": "image_size",
      "label": "Size",
      "options": [
        {
          "value": "small",
          "label": "Small"
        },
        {
          "value": "medium",
          "label": "Medium"
        },
        {
          "value": "large",
          "label": "Large"
        },
        {
          "value": "fill",
          "label": "Fill screen"
        }
      ],
      "default": "large"
    },
    {
      "type": "checkbox",
      "id": "stack_images",
      "label": "Stack images on desktop",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_thumbnails",
      "label": "Show thumbnails on desktop",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_image_zoom",
      "label": "Enable zoom",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_video_looping",
      "label": "Enable video looping",
      "default": false
    }
  ]
}
{% endschema %}
