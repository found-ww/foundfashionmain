{%- assign hand_picked_count = 0 -%}

{%- for block in section.blocks -%}
  {%- if block.settings.product != blank -%}
    {%- assign hand_picked_count = hand_picked_count | plus: 1 -%}
  {%- endif -%}
{%- endfor -%}

{%- capture flickity_options -%}
{
  "prevNextButtons": true,
  "pageDots": false,
  "wrapAround": false,
  "contain": true,
  "cellAlign": "center",
  "watchCSS": true,
  "dragThreshold": 8,
  "groupCells": true,
  "arrowShape": {"x0": 20, "x1": 60, "y1": 40, "x2": 60, "y2": 35, "x3": 25}
}
{%- endcapture -%}

{%- comment -%} Gather same-collection products when no hand-picked set {%- endcomment -%}
{%- assign ppv1_rec_products = '' -%}
{%- assign ppv1_rec_count = 0 -%}
{%- if hand_picked_count == 0 -%}
  {%- assign ppv1_collection = product.collections.first -%}
{%- endif -%}

<section class="Section Section--spacingNormal" data-section-id="{{ section.id }}" data-section-type="product-recommendations">
  {%- if section.settings.heading != blank -%}
    <header class="SectionHeader SectionHeader--center">
      <div class="Container">
        <h3 class="SectionHeader__Heading Heading u-h3">{{ section.settings.heading | escape }}</h3>
      </div>
    </header>
  {%- endif -%}

  <div class="ProductRecommendations">
    {%- if hand_picked_count > 0 -%}
      <div class="ProductListWrapper">
        <div class="ProductList ProductList--carousel Carousel" data-flickity-config='{{ flickity_options }}'>
          {%- for block in section.blocks -%}
            {%- assign rec_product = block.settings.product -%}
            {%- if rec_product != blank -%}
              <div class="Carousel__Cell">
                {% render 'product-item', product: rec_product, show_product_info: section.settings.show_product_info, show_vendor: section.settings.show_vendor, show_color_swatch: section.settings.show_color_swatch, show_labels: true %}
              </div>
            {%- endif -%}
          {%- endfor -%}
        </div>
      </div>

    {%- elsif ppv1_collection != blank -%}
      {%- comment -%} Show up to 8 products from the same collection, excluding current product {%- endcomment -%}
      {%- assign ppv1_rec_items = '' -%}
      {%- assign ppv1_rec_count = 0 -%}
      {%- for col_product in ppv1_collection.products limit: 20 -%}
        {%- unless col_product.id == product.id -%}
          {%- if ppv1_rec_count < 8 -%}
            {%- assign ppv1_rec_count = ppv1_rec_count | plus: 1 -%}
          {%- endif -%}
        {%- endunless -%}
      {%- endfor -%}

      {%- if ppv1_rec_count > 0 -%}
        <div class="ProductListWrapper">
          <div class="ProductList ProductList--carousel Carousel" data-flickity-config='{{ flickity_options }}'>
            {%- assign rendered = 0 -%}
            {%- for col_product in ppv1_collection.products limit: 20 -%}
              {%- unless col_product.id == product.id -%}
                {%- if rendered < 8 -%}
                  <div class="Carousel__Cell">
                    {% render 'product-item', product: col_product, show_product_info: section.settings.show_product_info, show_vendor: section.settings.show_vendor, show_color_swatch: section.settings.show_color_swatch, show_labels: true %}
                  </div>
                  {%- assign rendered = rendered | plus: 1 -%}
                {%- endif -%}
              {%- endunless -%}
            {%- endfor -%}
          </div>
        </div>
      {%- else -%}
        <style>#shopify-section-{{ section.id }} { display: none; }</style>
      {%- endif -%}

    {%- else -%}
      <style>#shopify-section-{{ section.id }} { display: none; }</style>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Product recommendations",
  "class": "",
  "templates": ["product"],
  "max_blocks": 10,
  "blocks": [
    {
      "type": "product",
      "name": "Product",
      "settings": [
        {
          "type": "paragraph",
          "content": "Manually selected products override same-collection recommendations."
        },
        {
          "type": "product",
          "id": "product",
          "label": "Product"
        }
      ]
    }
  ],
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "You may also like"
    },
    {
      "type": "checkbox",
      "id": "show_product_info",
      "label": "Show product info",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_vendor",
      "label": "Show vendor",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_color_swatch",
      "label": "Show color swatch",
      "info": "Some colors appear white? [Learn more](http://support.maestrooo.com/article/80-product-upgrading-custom-color-for-color-swatch).",
      "default": false
    }
  ],
  "presets": [
    {
      "category": "Product",
      "name": "Product recommendations"
    }
  ]
}
{% endschema %}
